class PropertyReport {
    
    constructor(id, property_location, report) {
      this.id = id;
      this.date = new Date();
      this.property_location = property_location;
      this.report = report;
    }
  }